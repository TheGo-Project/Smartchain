apiVersion: v1
kind: Service
metadata:
  name: geth-service
spec:
  selector:
    app: geth
  ports:
  - name: json-rpc
    protocol: TCP
    port: 8545  # JSON-RPC
    targetPort: 8545
  - name: p2p
    protocol: TCP
    port: 30303 # P2P
    targetPort: 30303
